export enum StatisticCardTypes {
    DARK = 'dark',
    GRADIENT = 'gradient',
}

export enum CompanyStatus {
    ACTIVE = 'active',
    NOT_ACTIVE = 'notActive',
    PENDING = 'pending',
    SUSPENDED = 'suspended',
}
